class Solution {
    public int removeElement(int[] nums, int val) {
        int[] arr = new int[nums.length];
        for(int = 0; i < nums.length; i++){
            if(nums[i] == val){

            }
            else{
                arr[i] == nums[i];
            }
        }
        return arr;
    }
}